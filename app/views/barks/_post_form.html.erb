<%= form_for(@newTweet) do |f| %>

  <center
    <div class="field">
      <%= f.label :content %><br />
      <%= f.text_field :content, autofocus: true %>
    </div>


    <div class="actions">
      <%= f.submit "Add Bark" %>
    </div>
    </center>
  <% end %>



  <!-- <% if @newTweet.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@newTweet.errors.count, "error") %> prohibited this tweet from being saved:</h2>

        <ul>
          <% @newTweet.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <%= f.hidden_field :user_id, value: current_user.id, readonly: true %>

    <div class="field">
      <%= f.label :message %><br>
      <%= f.text_area :message %>
    </div>
    <div class="field">
      <p class="post-as"> barking as: <span><%= current_user.username %></span></p>
    </div>

    <div class="actions">
      <%= f.submit %>
    </div>
